#include <stdio.h>

int main(){
    int t,n;
    int a, b;
    scanf("%d", &t);
    for (int i=0; i<t; i++){
        scanf("%d", &n);
        int temp;
        while(temp==0){
            for (int j=2; j<n;j++)
        }
    }
}