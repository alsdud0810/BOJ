#include <stdio.h>

int main(){
    char arr[51];
    scanf("%s",arr);
    int l=strlen(arr);
    int sum=0;
    for (int i=0; i<l; i++){
        if (arr[i]=='+'||arr[i]=='-'){

        }
        else{
            sum+=
        }
    }
}